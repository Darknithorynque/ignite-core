server:
  port: 8084
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ignite-core
    username: root
    password: "1234"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
